# Railway 배포 설정이 있는 곳

`railway.json`은 Railway가 deprecated 처리했고 **읽히지 않는다**. 이 저장소에 있던 그 파일은
start 명령·헬스체크·재시작 정책을 담고 있었지만 배포에 아무 영향을 주지 않았고, 설정이 적용된
것처럼 보이게 만들어 원인 추적을 어렵게 했다. 그래서 삭제했다.

현재 배포 설정은 Railway 서비스 설정에 직접 들어 있다.

| 항목 | 값 |
|---|---|
| 빌더 | Dockerfile (`Dockerfile`) |
| Start 명령 | `npx prisma migrate deploy && node tools/start-railway.mjs` |
| 헬스체크 | `/api/health`, 300초 |
| 재시작 | ON_FAILURE, 최대 5회 |
| 볼륨 | `/data` (첨부파일, `STORAGE_LOCAL_DIR=/data/storage`) |

앞으로 설정을 코드로 관리하려면 Railway의 Infrastructure as Code(`.railway/railway.ts`)를
쓴다. `railway.json`을 다시 만들지 말 것 — 조용히 무시된다.

시드(`npx tsx prisma/seed.ts`)는 start 명령에 넣지 않는다. 멱등하긴 하지만 매 기동마다
역할 권한을 프리셋으로 되돌려, 관리자가 `/system/roles`에서 조정한 권한을 지운다. 최초 1회만
수동으로 실행한다.
